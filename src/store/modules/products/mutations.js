export default {
  SET_ALBUMS (state, albums) {
    state.albums = albums
  },

  SET_PHOTOS (state, photos) {
    state.photos = photos
  },

  SET_LOADING (state, loading) {
    state.loading = loading
  }
}
